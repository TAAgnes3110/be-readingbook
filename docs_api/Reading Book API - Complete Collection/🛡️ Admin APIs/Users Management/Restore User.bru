meta {
  name: Restore User
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/admin/users/{{userId}}/restore
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{authToken}}
}

settings {
  encodeUrl: true
}
