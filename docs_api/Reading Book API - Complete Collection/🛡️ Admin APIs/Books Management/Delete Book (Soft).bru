meta {
  name: Delete Book (Soft)
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/admin/books/{{bookId}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{authToken}}
}

settings {
  encodeUrl: true
}
