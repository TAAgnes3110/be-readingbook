meta {
  name: Delete Book (Hard)
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/admin/books/{{bookId}}/hard
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{authToken}}
}

settings {
  encodeUrl: true
}
