meta {
  name: Restore Category
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/admin/categories/{{categoryId}}/restore
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{authToken}}
}

settings {
  encodeUrl: true
}
