meta {
  name: Delete Category (Hard)
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/admin/categories/{{categoryId}}/hard
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{authToken}}
}

settings {
  encodeUrl: true
}
