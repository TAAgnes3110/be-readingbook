meta {
  name: Remove Favorite Book
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/users/{{userId}}/favorites/{{bookId}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{authToken}}
}

settings {
  encodeUrl: true
}
