meta {
  name: Delete Bookmark
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/history/{{userId}}/bookmark/{{bookId}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{authToken}}
}

settings {
  encodeUrl: true
}
